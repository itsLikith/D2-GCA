package types

// GridPoint represents a single evaluation point in the coverage grid.
type GridPoint struct {
	// EastingNM is the E coordinate (NM).
	EastingNM float64

	// NorthingNM is the N coordinate (NM).
	NorthingNM float64
}
